# IDE
.idea/

# Maven build output
target/

# Application log
application.log

# Local environment file — contains real credentials, never commit
.env

# Archive folder — historical files preserved in git history, not needed in the active tree
archive/
