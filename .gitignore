# IDE
.idea/

# Maven build output
target/

# Application log
application.log

# Local environment file — contains real credentials, never commit
.env

# Archive sub-directories that must not be committed
# (git history already preserves the old source tree)
archive/.idea/
archive/uploads/
archive/Project_old/
